{
    "errors": [
        {% for error in errors %}
            {
                "field": "{{error.propertyPath}}",
                "message": "{{error.message}}"
            }
            {% if not loop.last %}
                ,
            {% endif %}
        {% endfor %}
    ]
}