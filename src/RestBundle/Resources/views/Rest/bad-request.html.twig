{
    "errors": [
        {% for error in errors %}
            {
                "field": "{{error.propertyPath}}",
                "message": "{{error.message}}",
                "lol": "lala"
            }
            {% if not loop.last %}
                ,
            {% endif %}
        {% endfor %}
    ]
}