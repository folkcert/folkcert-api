parameters:
  credentials-service.class: CredentialsBundle\Services\CredentialsService

services:
  credentials_service:
    class: '%credentials-service.class%'
    arguments: ['@kernel', '%folkcert_user_tokens%']
